# Palmistry AI - Integration API

A temporary FastAPI integration API for the Palmistry AI module.

The current implementation uses a dummy palmistry model for frontend integration and testing. The dummy model will later be replaced by the real ML model.

## Setup

Create and activate a virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate